schema_version: 1
detector:
  backend: euclidean_cluster
  model_subdir: models/autoware
  build_only: false
tracker:
  backend: ab3dmot
occupancy:
  static_enabled: true
  # Prediction/dynamic OGM integration is deliberately outside V1.
  dynamic_enabled: false
  publish_combined: false
